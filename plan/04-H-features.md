# 주요 기능 — [인사팀]

> 문서: 04-H-features.md · 상태: 초안(design-abstract.md 대조 반영) · 채택한 [제안] 0개 · 마지막 갱신: 오늘
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

> 비고: 이 문서는 03-H-requirements.md의 R1 ~ R9, R11~R45(43개 채택 확정, R10은 03에서 이미 제외)를 근거로 코치가 작성한 초안이다(전 행 원칙적으로 `[제안]`, 단 design-abstract.md에 실제로 있는 화면·요소·핸들러로 근거를 댈 수 있는 칸은 `[제안]` 대신 "설계 근거: <화면명>·<요소/핸들러>(design-abstract.md)"로 출처를 밝혔다 — 이는 코치의 짐작이 아니라 이미 존재하는 화면 설계 문서에서 가져온 사실이기 때문이다). 하나의 R에서 여러 F가 나오지 않고, 대신 성격이 같은 R 여러 개를 하나의 F로 묶은 경우가 있다(R8+R9, R15+R16, R33+R40, R36+R41+R42). 정책(P) 열은 05 정책 문서가 아직 없어 비워 두었고, 우선순위 열은 팀 결정이 없어 모든 행이 `[?]`이며, 인수 조건 열은 규칙대로 비워 두었다. R26("규정 위반" 판단 기준)은 04(기능)가 아니라 05(정책)에서 다룰 사항이라는 팀 결정에 따라, R18·R45(실시간 외부 변동 자동 재조정 — 사용하지 않기로 결정)는 스코프 제외에 따라 04에는 대응하는 F를 두지 않는다(F22 다음 F24, F14 다음 F16으로 F23·F15 결번) — 아래 "검토했으나 제외" 표 참고.

> 갱신 메모(코치, 이전 회차): 03-H-requirements.md R13 문면이 "AI가 일정 내용을 바로 수정"에서 "AI가 일정을 제안"으로 바뀜에 따라 F11의 결과·예외를 다시 썼다. F23(R26)은 기능이 아니라 규칙(제약)이라 05로 이관하기로 팀이 정해 04 표에서 뺐다. 이 두 결정은 이번 회차에서도 그대로 유지한다.

> 갱신 메모(코치, 오늘 — design-abstract.md 대조): 지금까지 트리거·입력 칸의 상당수가 코치의 순수한 추정(`[제안](근거: OO 기반 코치 추정)`)이었다. 이번에 design-abstract.md(Claude Design 프로토타입의 실제 화면·요소·핸들러 정리 문서)와 한 줄씩 대조해, 실제 버튼 라벨·핸들러명으로 바꿔치기할 수 있는 곳은 전부 바꿨다(예: F2의 "기준 선택"→세그먼트 `최단시간`/`최단거리`·`setCriteria`, F14의 "변수 추가"→상황 변경 팝업의 실제 옵션·`applySituation`). 이렇게 바꾼 칸은 `[제안]` 대신 "설계 근거: ..."로 표시했다. 반대로 03·04의 요구 문면과 design-abstract.md의 실제 화면이 서로 어긋나는 지점(예: "동선 확정" 버튼이 화면에 없음)도 다수 발견했다 — 억지로 끼워 맞추지 않고 각 F의 결과 칸에 **불일치**로 명시했고, 아래 "설계 불일치 — 팀 결정 필요" 절에 번호를 매겨 모았다. 대중교통(F4)·경로기준(F2)·변수반영(F14)·희망장소추가(F8)·내일정저장(F24~F27)·권한요청/승인(F30~F34)·여행지탐색(F35)·회원탈퇴(F38) 등은 design-abstract.md에 구체적 화면·핸들러가 있어 근거를 크게 보강했다. 반대로 개인 일정(F1)·휴무일 반영(F3)·AI 생성 실패 시 재시도(F6)·관리자/에디터 화면(F19~F22)·실시간 동기화류(F12·F16·F26)는 design-abstract.md에도 대응 화면이 없거나(정적 프로토타입이라 서버 동기화 자체가 안 보임) 확인이 안 되어 `[?]`/`[제안]`을 그대로 두거나 새로 추가했다.

> 갱신 메모(팀 결정 로그, 2026-09-16): "설계 불일치 — 팀 결정 필요" 절의 11개 항목 중 진행 상황 — 11번(F38/R44 탈퇴 정책)은 "화면 문구(전부 삭제)가 맞다"로 해결, 02·03을 정정. 1번(F9/R11 동선 확정)은 "확정 개념을 없애고 상시 공유"로 해결, 02·03·04를 정정. 2번(F11/R13 AI 제안)은 "참고용 텍스트일 뿐 자동 반영 없음"으로 해결, 03 R13을 정정. 4번(F31·F34/R35·R36 권한요청 알림)은 "지금의 pull 방식(권한 관리 화면) 채택"으로 해결, 02 S19·03 R35·R36을 정정. 5번(F17/R20 후기 위치)은 "커뮤니티 화면을 R20 충족으로 본다"로 해결, 02 S13·03 R20을 정정. 6번(F26/R29 저장본 동기화)은 "스냅샷 방식 채택"으로 해결, 02 S17·03 R29(제약으로 유형 변경, 문면 뒤집음)를 정정. 7번(F29/R31 게스트 로그인 시점)은 "지금의 지연 로그인 채택"으로 해결, 02 S18·03 R31을 정정. 8번(F15/R18,R45 재조정 배너)은 "해당 기능(R18 실시간 외부 변동 자동 재조정) 자체를 사용하지 않는다"로 해결 — R18·R45·F15와 관련 발산 질문을 스코프에서 전부 빼고 02 S12 To-Be·03·04 "검토했으나 제외" 표에 기록. 3·9·10번(F19~22, F6, F1)은 팀 결정이 필요한 갈림길이 아니라 단순 미설계 화면(9·10번)이거나 우선순위 문제(3번)로 재분류. **11개 항목 전부 처리 완료.**

> 그룹 검토 로그(팀, 2026-09-16 ~ ): 기능 표를 주제별로 묶어 검토 중. 셀 안의 `[제안](근거: ...)` 태그는 그대로 두되(추적용), 그룹이 "일괄 승인"되면 여기에 기록하고 별도로 태그를 지우지는 않는다(팀이 명시적으로 문면을 고친 항목만 그 자리에서 직접 수정). **F1~F7 그룹: 일괄 승인.** **F8~F12 그룹: 승인.** 단 F12(보기/편집 링크 구분)는 별도 논의 — 링크별 고정 권한 방식을 유지하기로 하고, 보기 링크 사용자는 편집 요청 자체를 못 하도록 추가 개발이 필요하다는 것을 F12 행에 기록(05에서 규칙으로 재정리 예정). **F13~F16 그룹: 일괄 승인.** **F17~F22 그룹: 일괄 승인**(F19~F22는 초안만 두고 실제 착수 여부는 ⑥ 우선순위 라운드에서 결정). **F24~F28 그룹: 일괄 승인.**

## 기능

| F | R | 행위자 | 트리거 | 입력 | 결과(보이는 것) | 예외(보이는 것) | 정책(P) | 우선순위 | 인수 조건 |
| - | - | ----- | ----- | ---- | -------------- | -------------- | ------- | ------- | ------- |
| F1 | R1 | A1 | 사용자가 개인 일정 입력 화면에서 출발·도착 시간 등을 입력하고 저장한다 [제안](근거: R1) — design-abstract.md 전체를 확인했으나 이 "개인별 출발·도착 시간 입력"에 대응하는 화면·요소를 찾지 못했다(경로 플래너의 "체류시간 −/+"는 한 방문지에 머무는 시간이지 참가자 개인의 출발·도착 시간이 아니다) | 출발지·도착지·출발시간·도착시간 등 개인 일정 정보 [제안](근거: R1) | 입력한 개인 일정이 전체 동선(일정)에 자동으로 반영되어 화면에 표시된다 [제안](근거: R1). 설계 예정(2026-09-16 팀 확인): 한 사람(A1)이 자신의 개인 시간 제약을 입력하는 단순 기능이며(여러 A1의 입력을 취합하는 게 아님, 02 S1과 일치), 방향을 정할 문제가 아니라 아직 화면이 안 만들어졌을 뿐이다. 별도 결정 없이 이후 화면 설계 단계에서 만들면 된다 | 필수 항목 누락 또는 시간 형식 오류 시 저장이 되지 않고 오류 안내가 표시된다 [제안](근거: R1 문면엔 없으나 입력값 검증 필요성에서 도출) | | [?] | |
| F2 | R2 | A1 | 사용자가 경로 플래너 메인 화면의 경로 기준 세그먼트 `최단시간` 또는 `최단거리`를 클릭한다(설계 근거: 경로 플래너 메인·세그먼트(2) "경로 기준"·`setCriteria`, design-abstract.md) | 선택한 기준값(`최단시간`/`최단거리`)(설계 근거: 경로 플래너 메인, design-abstract.md) | `criteria` 상태가 바뀌고, 아직 조회하지 않은 구간을 그 기준으로 백그라운드에서 재조회해 화면에 반영한다(설계 근거: 경로 플래너 메인·`setCriteria(c)`, design-abstract.md) | 선택한 기준으로 계산 가능한 동선이 없을 때 어떤 안내가 뜨는지는 design-abstract.md 상호작용 목록에 없다 [?](근거: 실패 케이스 기술 없음) | | [?] | |
| F3 | R3 | 시스템(대상: A1) | 동선 생성·계산 시점에 시스템이 자동으로 각 장소의 휴무일·영업시간 데이터를 반영한다 [제안](근거: R3) | 방문 장소 목록, 각 장소의 휴무일·브레이크타임·영업시간 데이터 [제안](근거: R3) | 휴무일·브레이크타임을 반영해 조정된 동선이 화면에 표시된다 [제안](근거: R3). design-abstract.md의 방문지 카드 요소에도 휴무일·영업시간 관련 필드는 없다(카테고리 칩·메모·체류시간만 있다) — 데이터 자체가 없다는 02·03의 상황과 일치한다 | 영업시간 정보가 없는 장소가 포함된 경우 어떻게 처리할지는 정해지지 않았다 [?](근거: 02·03·design-abstract.md 모두 이 경우를 다루지 않음) | | [?] | |
| F4 | R4 | A1 | 사용자가 방문지 카드에 펼쳐진 카카오맵 길찾기 링크를 클릭한다(설계 근거: 경로 플래너 메인·카카오맵 길찾기 링크·`onKakaoDirectionsClick`, design-abstract.md) | 없음(현재 표시된 구간의 출발지·도착지 좌표)(설계 근거: 경로 플래너 메인, design-abstract.md) | 외부 카카오맵으로 연결되어 대중교통 등 상세 길찾기 정보를 확인할 수 있다(설계 근거: 경로 플래너 메인·`onKakaoDirectionsClick`, design-abstract.md) | 좌표가 없으면 `e.preventDefault()`로 이동 자체가 차단된다(설계 근거: 경로 플래너 메인·`onKakaoDirectionsClick`, design-abstract.md) | | [?] | |
| F5 | R5 | A1 | 사용자가 "AI 일정 생성" 화면에서 Step0 ~ 5의 여행 날짜·지역·스타일·관심사·동행·이동방식을 차례로 선택하고 마지막 단계에서 `AI 일정 생성하기` 버튼을 클릭한다(설계 근거: AI 일정 생성 화면·Step0~5 각 칩·`goNext`→`generate()`, design-abstract.md) | 여행 날짜, 지역(또는 커스텀 입력), 여행 스타일(여유롭게/알차게/인기 장소 위주/조용하게), 관심사(다중선택: 맛집/카페/관광/쇼핑/자연/문화/체험), 동행(혼자/연인/친구/가족), 이동방식(도보/대중교통/자동차/자전거)(설계 근거: AI 일정 생성 화면·Step1~5, design-abstract.md) | Step6에서 스피너와 "AI가 최적의 일정을 만들고 있어요..." 문구가 1.4초간 표시된 뒤, Step7에서 요약문·통계(방문 장소/총 이동시간/총 이동거리)·장소 리스트가 표시된다(설계 근거: AI 일정 생성 화면·Step6~7·`generate()`→`buildResult()`, design-abstract.md) | design-abstract.md의 `generate()`는 1.4초 뒤 목업 결과를 항상 계산해 보여주는 것으로 되어 있어(고정 지연, 실패 케이스 없음), 조건 입력이 불완전할 때 어떤 안내가 뜨는지는 design-abstract.md에 없다 [?] | | [?] | |
| F6 | R6 | A1 | AI 생성 결과가 실패·결과 없음·오류 상태일 때 사용자가 "재시도" 버튼을 클릭한다 [제안](근거: R6) | 없음(기존 입력 조건 재사용) [제안](근거: R6) | 에러 메시지 안내와 함께 재시도가 진행되고 새 결과가 표시된다 [제안](근거: R6). 설계 예정(2026-09-16 팀 확인): design-abstract.md의 AI 일정 생성 화면에는 생성 실패·결과 없음·오류에 대한 화면(에러 메시지+재시도 버튼) 자체가 없다. Step7 결과 화면의 `다시 추천받기`(regenerate)는 결과가 이미 나온 뒤 같은 조건으로 재생성하는 기능(F7·R7)이지, 생성이 실패했을 때 보여줄 에러 화면과는 다르다 — 다만 실패 처리 자체는 방향을 고를 문제가 아니라 당연히 있어야 하는 기본 예외 처리라, 별도 팀 결정 없이 이후 화면 설계 단계에서 만들면 된다 | 재시도도 반복해서 실패하면 동일한 에러 메시지와 재시도 버튼이 다시 표시된다 [제안](근거: R6 기반 코치 추정) | | [?] | |
| F7 | R7 | A1 | 사용자가 AI 일정 생성 결과(Step7) 화면에서 `다시 추천받기` 버튼을 클릭한다(설계 근거: AI 일정 생성 화면·Step7·`regenerate`, design-abstract.md) | 없음(기존 선택 조건 재사용)(설계 근거: AI 일정 생성 화면, design-abstract.md) | `generate()`가 재실행되어 같은 조건으로 재계산된 새 결과가 화면에 표시된다(설계 근거: AI 일정 생성 화면·`regenerate`(`generate` 재실행), design-abstract.md) | 재생성 실패 시의 화면은 design-abstract.md에 없다(F6과 동일한 공백) [?] | | [?] | |
| F8 | R8, R9 | A1, A2 | 편집 권한이 있는 사용자(A1 또는 A2)가 경로 플래너 메인 하단의 주소 검색 입력(`placeholder="주소를 검색하여 추가하기"`)에 주소를 입력해 Enter를 누르거나 `추가`를 클릭한다. 검색으로 고른 장소가 아니면 "방문지 추가 확인 팝업"이 열리고 그 팝업의 `추가`를 클릭한다(설계 근거: 경로 플래너 메인·주소 검색+`addPlace`, 방문지 추가 확인 팝업·`confirmAddPlace`, design-abstract.md) | 주소(검색어), 팝업에서 확인·수정하는 이름·주소(설계 근거: 방문지 추가 확인 팝업, design-abstract.md) | `confirmAddPlace`가 실제 Kakao 지오코딩(`geocodePlace`)을 호출한 뒤 방문지를 추가하고 활동 로그·토스트가 표시된다. 방문지가 하나도 없는 빈 상태에서는 `＋` 아이콘으로 `quickAddPlace`가 즉시 추가한다(설계 근거: 경로 플래너 메인·빈 상태 `＋`·`quickAddPlace`, 방문지 추가 확인 팝업·`confirmAddPlace`, design-abstract.md) | 좌표 조회(지오코딩)에 실패해도 방문지는 그대로 추가된다(설계 근거: 방문지 추가 확인 팝업 "좌표 실패해도 추가", design-abstract.md). 이미 추가된 장소를 중복 추가하려 할 때의 안내는 design-abstract.md에 없다 [?] | | [?] | |
| F9 | R11 | A1 | A1(또는 `canManageInvite` 권한자)이 경로 플래너 메인에 상시 존재하는 `+ 일행 초대` 버튼을 클릭한다(설계 근거: 경로 플래너 메인·`+ 일행 초대`·`toggleInvite`, design-abstract.md). 2026-09-16 팀 결정으로 "동선 확정" 단계는 없앴다 | 없음(현재 일정 상태) — 초대 패널이 열리면 권한 드롭다운에서 편집 가능/보기 전용 중 권한을 고른다(설계 근거: 경로 플래너 메인(초대 패널)·권한 드롭다운·`togglePermissionMenu`/`setPermission(p)`, design-abstract.md) | 초대 링크가 인라인으로 표시되고 `복사`를 클릭하면 `copyInvite`가 실행되어 링크가 클립보드에 복사되며 버튼 라벨이 1.2초간 `복사됨`으로 바뀐다(설계 근거: 경로 플래너 메인(초대 패널)·`복사`·`copyInvite`, design-abstract.md). `+ 일행 초대`는 일정 생성 직후부터 상시 존재하며 "확정"을 거쳐야 공유가 가능해지는 구조가 아니다(2026-09-16 팀 결정: "확정" 개념을 없애고 상시 공유로 03·02 문면을 정정함) | 링크 복사 실패 시 안내가 있는지는 design-abstract.md에 없다 [?] | | [?] | |
| F10 | R12 | 시스템 | 일정이 종료(S21, F36)되는 시점에 시스템이 자동으로 감지한다 [제안](근거: R12) | 없음 [제안](근거: R12) | 별도 조치 없이 공유 링크가 함께 만료된다 [제안](근거: R12). F9에서 확인했듯 공유 방식에 "확정"이라는 별도 관문이 없으므로 "일정 진행 중 계속 유효"는 자연스럽게 성립하지만(공유 자체를 막을 단계가 없음), 종료 시점에 링크를 자동으로 만료시키는 코드나 화면(만료 안내 등)은 design-abstract.md에 없다 [?] | 만료된 링크로 접속하면 "만료된 링크입니다" 안내가 표시된다 [제안](근거: R12 기반 코치 추정, design-abstract.md에 대응 화면 없음) | | [?] | |
| F11 | R13 | A1, A2 | 사용자가 AI 도우미 패널(FAB `✨ AI 도우미`로 열림)의 입력창에 자연어 요청을 입력해 Enter 또는 `전송`을 클릭한다. 또는 제시된 추천 칩을 클릭한다(설계 근거: 경로 플래너 - AI 도우미 패널·입력+`전송`·`onAiInput`/`sendAiMessage`, 추천 칩·`onSuggestionClick`, `✨ AI 도우미` FAB·`toggleAi`, design-abstract.md) | 자연어 요청 텍스트(현재 경로 컨텍스트 `aiContextLabel` 포함)(설계 근거: 경로 플래너 - AI 도우미 패널, design-abstract.md) | `/api/chat`으로 POST 요청을 보내고 응답을 대화 목록에 참고용 텍스트로 추가한다(설계 근거: 경로 플래너 - AI 도우미 패널·`sendAiMessage`, design-abstract.md). 2026-09-16 팀 결정: 이 제안은 참고용일 뿐이며 실제 방문지·동선에 자동으로 반영되지 않는다(수락·적용 절차를 새로 만들지 않기로 함) — 03 R13 문면을 이에 맞춰 정정했다 | AI 응답 실패 시 에러 메시지가 표시된다(설계 근거: 경로 플래너 - AI 도우미 패널·`sendAiMessage`("실패시 에러 메시지"), design-abstract.md) | | [?] | |
| F12 | R14 | A3, A5 | 사용자가 보기 권한 링크로 접속한다 [제안](근거: R14) | 링크 URL [제안](근거: R14) | 로그인 여부와 무관하게 실시간으로 갱신되는 동선이 화면에 표시된다 [제안](근거: R14) — design-abstract.md는 localStorage 기반 정적 프로토타입이라 서버와의 실시간 동기화를 보여주는 코드가 없어, 이 부분은 design 근거로 확인할 수 없다 | 링크가 만료되었거나 일정이 삭제된 경우 접근할 수 없다는 안내가 표시된다(R43·F37과 연결)(2026-09-16 팀 결정: 보기 링크와 편집 링크는 생성 시점에 권한이 고정된 서로 다른 링크다 — F12 그룹 검토 뒤 각주 참고) [제안](근거: R14 기반 코치 추정) | | [?] | |
| F13 | R15, R16 | A2 | A2(또는 `canEdit` 권한자)가 방문지 카드를 드래그해 순서를 바꾸거나, 방문지 수정 팝업에서 이름·주소를 바꾸거나, 카테고리 칩을 고르거나, 메모를 저장하거나, 체류시간 `−`/`+`를 클릭하거나, 구간 이동수단 칩을 클릭해 순환 변경하거나, 방문지를 추가·삭제한다(설계 근거: 경로 플래너 메인·드래그(`onDragStart`/`onDragOver`/`onDrop`/`onDragEnd`), 방문지 수정 팝업(`saveEditModal`), 카테고리 선택 팝업(`selectCategory`), 메모(`saveMemo`), 체류시간(`changeDuration`), 구간 이동수단(`cycleSegmentMode`), design-abstract.md) | 변경 내용(순서, 이름/주소, 카테고리, 메모, 체류시간 ±5분, 이동수단)(설계 근거: 위 각 요소, design-abstract.md) | 각 변경이 즉시 반영되고 대부분 활동 로그에 기록되며, 순서 변경 시 구간이 재계산되고 `routeCache`가 초기화된다(설계 근거: 경로 플래너 메인·드래그 결과 "방문 순서 변경, 구간 재계산, routeCache 초기화, 활동 로그", design-abstract.md). 여러 A2가 동시에 수정해도 즉시 반영되며, 별도 충돌 처리 UI는 design-abstract.md에 없다(02의 "충돌 처리 규칙 없음"과 일치) | 네트워크 오류 등으로 저장이 실패하는 경우의 안내는 design-abstract.md에 없다(정적 프로토타입이라 서버 저장 실패 케이스 자체가 없음) [?] | | [?] | |
| F14 | R17 | A1, A2 | 사용자가 요약 바의 `변수 추가`(canEdit) 버튼을 클릭해 "상황 변경(변수 추가) 팝업"을 열고, 짐 추가/기상 변화/일정 지연/교통 상황 악화 중 하나를 고른 뒤(기상 변화 선택 시 비/눈/태풍 중 하위 옵션도 고르고) 힘듦 정도(조금 불편/보통/매우 불편)를 선택해 `동선 재계산`을 클릭한다(설계 근거: 경로 플래너 메인·`변수 추가`, 경로 플래너 - 상황 변경(변수 추가) 팝업·`selectSituationVar`/`Sub`/`Severity`·`applySituation`, design-abstract.md) | 변수 항목(짐 추가/기상 변화/일정 지연/교통 상황 악화), 기상 변화의 하위 옵션(비/눈/태풍), 불편 정도(조금 불편/보통/매우 불편)(설계 근거: 경로 플래너 - 상황 변경 팝업, design-abstract.md) | `applySituationAdjustment()`가 800ms 로딩 후 방문지(places)·구간(segments)을 재계산하고 활동 로그·토스트가 표시된다(설계 근거: 경로 플래너 - 상황 변경 팝업·`applySituation`, design-abstract.md) | 재계산 실패 케이스는 design-abstract.md에 없다(800ms 후 항상 성공하는 것으로 되어 있음) [?] | | [?] | |
| F16 | R19 | A2, A3, A5 | 다른 사용자(A1/A2)가 일정을 변경한다 [제안](근거: R19) | 변경된 일정 데이터 [제안](근거: R19) | 접속 중인 모든 사용자 화면에 새로고침 없이 변경 사항이 즉시 반영된다 [제안](근거: R19) — design-abstract.md는 localStorage 기반 정적 프로토타입이라 여러 사용자 간 실시간 반영을 보여주는 코드가 없어 design 근거로 확인할 수 없다 | 연결이 끊기면 동기화가 지연되고 재연결 시 최신 상태로 갱신된다는 안내가 표시된다 [제안](근거: R19 기반 코치 추정) | | [?] | |
| F17 | R20 | A1, A2 | 사용자가 커뮤니티 화면의 사이드바 `여행 후기 쓰기`(신규) 또는 본인 게시물 `수정`(기존)을 클릭해 "글쓰기/수정 팝업"을 연다(설계 근거: 커뮤니티·사이드바 `여행 후기 쓰기`·`openComposer`, 본인 게시물 `수정`·`openEditComposer`, design-abstract.md). 2026-09-16 팀 결정으로 "동선 안에서 바로"가 아니라 "여행을 마친 뒤 커뮤니티 화면에서"로 02·03을 정정했다 | 사진, 여행지, 지역(select), 감상평(textarea, #태그 자동 강조)(설계 근거: 커뮤니티 - 글쓰기/수정 팝업, design-abstract.md) | `saveComposer`가 실행되어 신규 게시물이 추가되거나 기존 게시물이 갱신되어 커뮤니티 화면에 노출된다(설계 근거: 커뮤니티 - 글쓰기/수정 팝업·`saveComposer`, design-abstract.md) | 필수 항목 미입력 시 안내는 design-abstract.md에 없다 [?] | | [?] | |
| F18 | R21 | A4 | A4가 커뮤니티 화면에 진입해 검색·지역 필터·정렬(인기순/최신순/오래된순)·보기 전환(목록/사진)으로 조건을 좁힌다(설계 근거: 커뮤니티 화면·검색·지역 필터·정렬·보기전환, design-abstract.md) | 검색어, 지역 필터, 정렬 기준, 보기 모드(선택)(설계 근거: 커뮤니티 화면, design-abstract.md) | 조건에 맞는 게시물이 그리드(사진) 또는 리스트(목록) 형태로 표시되고 `더보기`로 추가 게시물을 볼 수 있다(설계 근거: 커뮤니티 화면·그리드뷰/리스트뷰·`더보기`(`showMorePosts`), design-abstract.md) | 조건에 맞는 게시물이 없으면 `{{ noPostsMessage }}`(검색결과 없음/게시물 없음)가 표시된다(설계 근거: 커뮤니티 화면·빈 상태 텍스트, design-abstract.md) | | [?] | |
| F19 | R22 | A7 | A7이 테마 기획 화면에서 트렌드 참고 도구를 실행한다 [제안](근거: R22). 설계 예정: design-abstract.md 전체에 A7(에디터) 전용 화면이나 "인기 검색어·SNS 트렌드 참고 도구"에 해당하는 화면·요소가 없다 — 관리자 화면 부재(F21·F22)와 같은 성격의 공백이다. 지금 설계할지 v2로 미룰지는 개별 질문이 아니라 ⑥ 우선순위 라운드에서 F19~F22를 묶어 정한다 | 검색 키워드(선택) [제안](근거: R22) | 인기 검색어·SNS 트렌드 목록이 화면에 표시된다 [제안](근거: R22) | 외부 트렌드 데이터 연동에 실패하면 오류 안내가 표시된다 [제안](근거: R22 기반 코치 추정) | | [?] | |
| F20 | R23 | A7 | A7이 "새 게시물 작성" 화면에서 글과 사진을 작성·편집한다 [제안](근거: R23). 설계 예정: design-abstract.md에 A7의 "여행지 탐색(Explore Destinations)" 콘텐츠 작성·편집 화면이 없다. 커뮤니티의 "글쓰기/수정 팝업"은 일반 사용자(A1/A2)의 여행 후기 작성용이지 A7의 테마별 게시물(여행지 탐색 카드) 작성용이 아니다 — F19~F22와 함께 ⑥ 우선순위 라운드에서 다룬다 | 글(텍스트), 사진 [제안](근거: R23) | 작성한 게시물이 저장·게시되어 여행지 탐색 화면에 노출된다 [제안](근거: R23) | 필수 항목(글 또는 사진)이 비어 있으면 게시가 되지 않고 안내가 표시된다 [제안](근거: R23 기반 코치 추정) | | [?] | |
| F21 | R24 | A7 | A7이 관리자 화면(신고 접수 현황)에 진입한다 [제안](근거: R24). 설계 예정: design-abstract.md의 "화면 목록" 전체를 확인한 결과 관리자·신고 처리에 해당하는 화면이 전혀 없다 — F19·F20·F22와 함께 ⑥ 우선순위 라운드에서 다룬다 | 없음 [제안](근거: R24) | 신고 접수된 게시물·댓글 목록이 한 화면에 표시된다 [제안](근거: R24) | 접수된 신고가 없으면 빈 목록이 표시된다 [제안](근거: R24 기반 코치 추정) | | [?] | |
| F22 | R25 | A7 | A7이 필터링 결과 화면에서 항목을 선택해 "삭제" 또는 "경고" 버튼을 클릭한다 [제안](근거: R25). 설계 예정: F21과 같은 이유로 design-abstract.md에 대응 화면이 없다 — F19·F20·F21과 함께 ⑥ 우선순위 라운드에서 다룬다 | 처리 대상 게시물·댓글, 처리 유형(삭제/경고) [제안](근거: R25) | 처리 결과가 즉시 반영되어 목록에서 상태가 바뀐 것이 표시된다 [제안](근거: R25) | 이미 처리(삭제)된 항목을 다시 처리하려 하면 안내가 표시된다 [제안](근거: R25 기반 코치 추정) | | [?] | |
| F24 | R27 | A3(로그인) | A3(로그인 상태)가 경로 플래너 메인 요약 바의 `내 일정에 저장` 버튼을 클릭한다(설계 근거: 경로 플래너 메인·요약 바 `내 일정에 저장`·`saveOrRemoveAction`, design-abstract.md) | 없음(현재 열람 중인 일정)(설계 근거: 경로 플래너 메인, design-abstract.md) | `saveCurrentRoute()`가 실행되어 그 시점의 일정이 localStorage(내 일정 목록)에 저장되고 devRole이 전환되며 토스트가 표시된다(설계 근거: 경로 플래너 메인·`saveOrRemoveAction`→`saveCurrentRoute()`, design-abstract.md) | 비로그인 상태에서 클릭하면 "로그인 필요 모달"("내 일정에 저장하려면 로그인이 필요해요" + `로그인`/`회원가입` 링크)이 뜬다(설계 근거: 경로 플래너 - 로그인 필요 모달, design-abstract.md) | | [?] | |
| F25 | R28 | A3(로그인) | A3가 "내 일정" 화면 카드의 제목(비편집 상태)을 클릭한다(설계 근거: 내 일정·카드 제목(비편집)·`startEdit`, design-abstract.md) | 새 제목(텍스트)(설계 근거: 내 일정, design-abstract.md) | 제목이 입력창으로 바뀌고, blur/Enter 시 `saveEdit()`가 localStorage에 저장한다(원본·다른 사람 저장본에는 영향 없음)(설계 근거: 내 일정·카드 제목 입력·`onEditSave`/`onEditKeyDown`, design-abstract.md) | Escape 키를 누르면 편집이 취소된다(설계 근거: 내 일정, design-abstract.md). 빈 제목으로 저장을 시도할 때의 안내는 design-abstract.md에 없다 [?] | | [?] | |
| F26 | R29 | 없음(자동 동기화 없음) | 원본 일정의 장소·동선 등 내용이 이후 바뀐다(설계 근거: 경로 플래너 메인·`saveCurrentRoute`는 저장 시점 상태를 localStorage에 복사해두는 스냅샷 방식이고, 원본과 라이브로 연결하는 코드는 없음, design-abstract.md) | 저장본은 저장한 시점 그대로 유지되며 자동으로 갱신되지 않는다(2026-09-16 팀 결정: R29 "실시간 반영"을 스냅샷 방식으로 뒤집음). 저장본을 열면(`loadSavedRoute`) 저장 시점의 데이터가 그대로 표시된다(설계 근거: 경로 플래너 - 내 일정 드로어·`loadSavedRoute`, design-abstract.md) | 없음(자동 갱신 자체가 없으므로 실패 케이스도 없음) | | [?] | |
| F27 | R30 | A3(로그인) | A3가 "내 일정" 카드의 삭제 아이콘을 클릭한다(설계 근거: 내 일정·카드 삭제 아이콘·`openDeleteConfirm`, design-abstract.md) | 없음(설계 근거: 내 일정, design-abstract.md) | "내 일정에서 삭제하시겠습니까?" 확인 모달에서 `내 일정에서 삭제`를 클릭하면 목록에서만 제거되고(원본 영향 없음) 토스트가 표시된다(설계 근거: 내 일정 - 삭제 확인 모달·`confirmDelete`, design-abstract.md) | 모달의 `취소`를 클릭하면 닫히고 아무 변화가 없다(설계 근거: 내 일정 - 삭제 확인 모달, design-abstract.md) | | [?] | |
| F28 | R39 | A1(트리거), A3(영향) | A1이 원본 일정을 삭제한다 [제안](근거: R39). 다른 사용자들이 저장해 둔 저장본까지 연쇄로 지우는 로직·화면은 design-abstract.md에서 찾지 못했다(정적 프로토타입이라 다중 사용자 저장본 연쇄 삭제 자체를 보여줄 수 없음) | 없음 [제안](근거: R39) | 그 원본을 저장했던 모든 사용자의 저장본이 함께 삭제된다 [제안](근거: R39) | 없음(이미 삭제 처리이므로 별도 예외 없음) [제안](근거: R39 기반 코치 추정) | | [?] | |
| F29 | R31 | A5 | A5(게스트)가 편집 링크로 접속한다(설계 근거: 경로 플래너 메인, design-abstract.md) | 링크 URL [제안](근거: R31) | 곧바로 Viewer(A3) 화면으로 일정을 볼 수 있다. 이후 `편집 권한 요청`(요약 바) 등 편집 관련 액션을 클릭하면 그 시점에 "로그인 필요 모달"(비로그인 시)이나 로그인 화면으로 이동한다(설계 근거: 경로 플래너 메인·`편집 권한 요청`·`requestEditPermission`, 경로 플래너 - 로그인 필요 모달, design-abstract.md). 2026-09-16 팀 결정으로 R31을 "일단 Viewer로 보여주고 편집 액션 시점에 로그인 요구"로 정정했다 | 로그인 화면 진입 자체가 실패(네트워크 오류 등)하면 오류 안내가 표시된다 [제안](근거: R31 기반 코치 추정) | | [?] | |
| F30 | R32 | A5→A3 | 뷰어(A3) 화면 요약 바에서 사용자가 `편집 권한 요청` 버튼을 클릭한다(설계 근거: 경로 플래너 메인·요약 바 `편집 권한 요청`(viewer)·`requestEditPermission`, design-abstract.md) — 이전 초안의 "로그인 완료 시점에 자동 전달"이라는 추정과 달리, design-abstract.md는 로그인 후에도 별도 버튼 클릭이 필요한 수동 요청 구조로 되어 있다 | 없음(설계 근거: 경로 플래너 메인, design-abstract.md) | `editRequestStatus='pending'`으로 바뀌고 토스트가 표시된다(설계 근거: 경로 플래너 메인·`requestEditPermission`, design-abstract.md) | 요청 전달 실패 시 안내는 design-abstract.md에 없다 [?] | | [?] | |
| F31 | R35 | A1 | A1이 요약 바의 `권한 관리` 버튼을 클릭해 "권한 관리 모달"을 직접 연다(설계 근거: 경로 플래너 메인·`권한 관리`·`openPermissionsModal`, design-abstract.md). 2026-09-16 팀 결정으로 push 알림 방식은 채택하지 않고 이 pull 방식을 그대로 쓰기로 했다(행위자도 "시스템(대상: A1)"에서 "A1"로 정정) | 없음 — 모달을 열면 편집 권한 요청 목록(닉네임 + `승인`/`거절`)이 표시된다(설계 근거: 경로 플래너 - 권한 관리 모달, design-abstract.md) | 요청자 닉네임이 표시된다(일정명·요청시간은 화면에 없어 R35 요구에서 뺌, 2026-09-16 정정) | 요청이 없으면 목록이 비어 있을 것으로 보이나 빈 상태 문구는 design-abstract.md에 없다 [?] | | [?] | |
| F32 | R33, R40 | A5→A3 | 편집 권한 요청(F30) 후 A1의 승인·거절을 기다리는 동안(설계 근거: 경로 플래너 메인, design-abstract.md) | 없음 | 요약 바에 `승인 대기 중` 표시가 뜨고, 그 외에는 Viewer 화면을 그대로 이용한다(설계 근거: 경로 플래너 메인·요약 바 `승인 대기 중` 표시, design-abstract.md) | 없음(대기 상태 자체가 예외 없는 지속 상태)(설계 근거: 경로 플래너 메인, design-abstract.md) | | [?] | |
| F33 | R34 | A2 | 이미 승인받은 A2가 로그아웃 후 같은 편집 링크로 재접속해 로그인한다 [제안](근거: R34). design-abstract.md의 Dev Role 칩(Creator/Editor/Viewer/Saved Viewer)은 화면 전환을 테스트하기 위한 개발용 스위치일 뿐, 실제 로그인·세션 유지 흐름을 보여주는 요소가 아니라 이 트리거는 design 근거로 대체하지 않고 [제안]으로 남긴다 | 로그인 정보 [제안](근거: R34) | 재승인 절차 없이 바로 편집 화면으로 들어간다 [제안](근거: R34) | 로그인 자체에 실패하면 로그인 오류 안내가 표시된다 [제안](근거: R34 기반 코치 추정) | | [?] | |
| F34 | R36, R41, R42 | A1 | A1이 "권한 관리 모달"의 요청 항목에서 `승인` 또는 `거절`을 클릭하면 "권한 요청 확인 모달"이 열리고, 그 모달에서 다시 `수락` 또는 `거절`을 클릭한다(설계 근거: 경로 플래너 - 권한 관리 모달·`askApproveRequest`/`askRejectRequest`, 경로 플래너 - 권한 요청 확인 모달·`confirmRequestAction`, design-abstract.md). R36을 "권한 관리 화면에서 승인/거절"로 정정(2026-09-16, F31과 동일한 pull 방식 결정) | 승인/거절 선택(2단계 확인)(설계 근거: 위 모달들, design-abstract.md) | 승인 시 `members`에 editor로 추가되고 요청 목록에서 제거된다(=즉시 A2가 됨). 거절 시 요청 목록에서 제거된다(=A3로 유지·전환)(설계 근거: 경로 플래너 - 권한 요청 확인 모달·`confirmRequestAction`, design-abstract.md). 한번 승인된 A2의 권한을 A1이 회수하는 버튼·동작은 design-abstract.md에 없다(=회수 불가 요구(R41)와 일치) | 이미 처리된 요청을 다시 처리하는 케이스의 안내는 design-abstract.md에 없다 [?] | | [?] | |
| F35 | R37 | A5, 회원 | 사용자가 "여행지 탐색" 화면에 진입해 지도 핀이나 지역 목록에서 지역을 클릭하거나 `전체`를 클릭한다(설계 근거: 여행지 탐색·지역 지도 핀/목록·`selectRegion`/`selectAll`, design-abstract.md) | 선택한 지역(또는 전체)(설계 근거: 여행지 탐색, design-abstract.md) | 로그인 여부와 무관하게 해당 지역의 여행지 카드(이미지·배지·이름·지역·설명·태그·`이 여행지로 일정 짜기 →`)가 표시된다(설계 근거: 여행지 탐색·여행지 카드, design-abstract.md) | 해당 지역에 추천 여행지가 없으면 "이 지역의 추천 여행지가 아직 없어요"가 표시된다(설계 근거: 여행지 탐색·빈 상태 텍스트, design-abstract.md) | | [?] | |
| F36 | R38 | 시스템(대상: A1, A2) | 시스템이 일정의 마지막 날짜 경과를 감지한다(자동, 사용자 조작 없음) [제안](근거: R38). "내 일정" 화면에 `예정된 일정`/`완료된 일정` 탭 구분은 있으나(설계 근거: 내 일정·탭(2)·`onSetStatusTab`, design-abstract.md), 완료된 일정을 `열기`로 열었을 때 실제로 편집이 막히는 읽기 전용 모드로 전환되는지는 design-abstract.md 상호작용 목록에 나타나 있지 않다 [?] | 없음 [제안](근거: R38) | 일정이 읽기 전용으로 전환되어 A1·A2 모두 더 이상 편집할 수 없다 [제안](근거: R38, 위 [?] 참고) | 종료 후 수정을 시도하면 "편집할 수 없습니다" 등 읽기 전용 안내가 표시된다 [제안](근거: R38 기반 코치 추정) | | [?] | |
| F37 | R43 | 시스템(대상: A2, A3) | A1이 원본 일정을 삭제하는 시점에 다른 사용자가 그 일정 화면에 실시간 접속해 있다 [제안](근거: R43). design-abstract.md는 단일 세션 기준 정적 프로토타입이라 다른 사용자 화면에 실시간으로 안내가 뜨는 코드는 찾지 못했다 | 없음 [제안](근거: R43) | 접속 중이던 A2/A3 화면에 "삭제된 일정입니다" 안내가 표시되고 더 이상 접근할 수 없다 [제안](근거: R43) | 삭제 이후 같은 링크로 재접속해도 동일한 안내가 표시된다 [제안](근거: R43 기반 코치 추정) | | [?] | |
| F38 | R44 | A1 | A1이 마이페이지에서 `회원 탈퇴`를 클릭해 탈퇴 절차(confirm→blocked(Creator 소유 일정 있을 때)→finalWarning→success)를 진행한다(설계 근거: 마이페이지·`회원 탈퇴`·`onOpenWithdraw`, 마이페이지 - 회원 탈퇴 모달(각 단계), design-abstract.md) | 동의 체크박스(각 단계)(설계 근거: 마이페이지 - 회원 탈퇴 모달, design-abstract.md) | design-abstract.md의 "회원 탈퇴 모달(finalWarning)" 경고문("회원 탈퇴 시 모든 일정, 기록들은 소멸되며...")이 그대로 정책이다 — 탈퇴는 원본 삭제(F28·R39)와 동일하게 처리되어, 소유했던 그룹 일정과 그 일정을 저장해 둔 다른 사용자들의 저장본까지 함께 삭제된다. 각 참가자의 로컬 저장 여부와는 무관하다(2026-09-16 팀 결정, R44 정정) | `deleteAccount()` 실패 시 에러가 표시된다(설계 근거: 마이페이지 - 회원 탈퇴 모달, design-abstract.md) | | [?] | |

## 설계 불일치 — 팀 결정 필요

> 아래는 02·03의 요구 문면과 design-abstract.md의 실제 화면·동작이 서로 어긋나는 지점이다. 화면 설계와 요구사항이 진짜로 충돌하는 경우만 추렸다(문구 확인성 질문 제외). 번호는 위 기능 표의 "**불일치**" 표시와 대응한다.
>
> 갱신(2026-09-16): 11개 중 3개(3·9·10번)는 다시 보니 방향이 갈리는 결정이 아니라 그냥 아직 안 만들어진 화면이거나(9·10번) 우선순위 문제(3번, ⑥ 라운드에서 다룸)라 재분류했고, 11번은 팀 결정으로 해결했다. 실제로 방향을 정해야 하는 것은 1·2·4·5·6·7·8번 6개다.

| 번호 | 대상 F/R | 무엇이 어긋나는가 | 팀이 정할 것 |
| -- | -------- | ---------------- | ------------ |
| 1 | F9 / R11 | ~~03·04는 "동선 확정" 버튼·단계를 전제하지만, design-abstract.md엔 그런 버튼이 없다~~ | **해결(2026-09-16 팀 결정)**: "확정" 개념을 없애고 일정 생성 즉시부터 상시 공유 가능으로 02·03·04를 정정했다 |
| 2 | F11 / R13 | design-abstract.md의 AI 도우미는 `/api/chat` 응답을 대화창에 텍스트로 추가하는 것으로 끝난다. 제안을 실제 방문지·동선에 적용(수락)하는 버튼이나 동작이 없다 | AI 제안을 일정에 반영하는 수락·적용 절차(버튼 등)를 새로 설계할지, 아니면 "제안은 참고용 텍스트일 뿐 자동 반영 없음"으로 요구사항을 다시 쓸지 |
| 3 | F19, F20, F21, F22 / R22, R23, R24, R25 | ~~design-abstract.md에는 A7(에디터)용 화면이 전혀 없다~~ | **재분류(2026-09-16)**: 지금 결정할 문제가 아니라 우선순위(MVP/v2) 문제라 개별 질문이 아니라 ⑥ 우선순위 2축 라운드에서 F19~F22를 묶어 다룬다 |
| 4 | F31, F34 / R35, R36 | ~~03은 "요청자 닉네임·일정명·요청시간이 담긴 알림"(push)을 요구하지만, design-abstract.md는 A1이 직접 여는 "권한 관리" 모달(pull, 닉네임만 표시)뿐이다~~ | **해결(2026-09-16 팀 결정)**: 지금의 pull 방식(권한 관리 화면)을 채택. 02 S19·03 R35·R36·04 F31을 "일정명·요청시간 없이 닉네임만, 권한 관리 화면에서 확인"으로 정정했다 |
| 5 | F17 / R20 | ~~02·03은 "동선 안에서 바로 후기를 남긴다"고 하지만, design-abstract.md의 후기 작성은 별도의 커뮤니티 화면에서만 가능하다~~ | **해결(2026-09-16 팀 결정)**: 커뮤니티 화면을 R20 충족으로 본다. 02 S13·03 R20·04 F17을 "여행을 마친 뒤 커뮤니티 화면에서"로 정정했다 |
| 6 | F26 / R29 | ~~R29는 "원본이 바뀌면 저장본에도 실시간 반영"을 요구하는데, design-abstract.md의 저장 기능은 스냅샷 방식으로 보인다~~ | **해결(2026-09-16 팀 결정)**: 스냅샷 방식 채택, R29 문면을 뒤집어 정정("저장 시점 그대로 유지되며 자동 갱신되지 않는다"). 02 S17·03 R29·04 F26을 정정했다 |
| 7 | F29 / R31 | ~~R31은 "즉시 로그인 화면으로 이동"을 요구하지만, design-abstract.md엔 즉시 리다이렉트가 안 보이고 개별 액션 시점에만 로그인 모달이 뜬다~~ | **해결(2026-09-16 팀 결정)**: 지금의 지연 로그인(일단 Viewer로 보여주고 편집 액션 시점에 로그인 요구) 채택. 02 S18·03 R31·04 F29를 정정했다 |
| 8 | F15 / R18, R45 | ~~design-abstract.md의 "급한 방문지가 뒤로 밀렸어요" 배너가 R18의 결과 화면인지, 수동 조작의 부수 안내인지 불명확~~ | **해결(2026-09-16 팀 결정)**: R18(실시간 외부 변동 자동 감지·재조정)은 사용하지 않는 기능이라 스코프에서 아예 뺐다. R18·R45·F15와 관련 발산 질문을 모두 제거하고, 02 S12는 To-Be를 "채택 안 함"으로 정정, 03·04 "검토했으나 제외" 표에 기록했다 |
| 9 | F6 / R6 | ~~R6은 "AI 생성 실패·결과 없음·오류 시 에러 안내+재시도"를 요구하지만, design-abstract.md의 AI 일정 생성 화면엔 이런 실패 화면이 없다~~ | **재분류(2026-09-16)**: 방향을 고를 문제가 아니라 당연히 있어야 하는 기본 예외 처리라 결정 불필요 — 이후 화면 설계 단계에서 만든다 |
| 10 | F1 / R1 | ~~R1(개인별 출발·도착 시간 입력)에 대응하는 화면 자체가 design-abstract.md에 없다~~ | **재분류(2026-09-16, 팀 확인)**: 한 사람이 자신의 개인 시간 제약을 입력하는 단순 기능(02 S1과 일치)이라 방향을 고를 문제가 아니다 — 결정 불필요, 이후 화면 설계 단계에서 만든다 |
| 11 | F38 / R44 | ~~design-abstract.md의 회원 탈퇴 모달(finalWarning) 경고문이 "회원 탈퇴 시 모든 일정, 기록들은 소멸되며..."라고 되어 있어, R44("그룹 일정은 삭제되지 않고 보존되며 읽기 전용으로 전환된다")와 정면으로 모순된다~~ | **해결(2026-09-16 팀 결정)**: 화면 문구가 맞다. 탈퇴는 원본 삭제(R39)와 동일하게 처리되어 그룹 일정과 그 저장본까지 함께 삭제된다. 02-H-workflow.md 분기 절·03-H-requirements.md R44를 이 결정에 맞게 정정했다 |
| 12 | F12 / R14 | design-abstract.md의 "편집 권한 요청" 버튼은 devRole=Viewer면 어느 링크로 들어왔든 상관없이 보이는 구조라, "보기 링크"와 "편집 링크"가 실제로 구분되는지 불명확했다 | **해결(2026-09-16 팀 결정)**: 보기 링크·편집 링크는 생성 시점에 권한이 고정된 서로 다른 링크로 유지한다(F9의 권한 드롭다운에서 정한 대로). 보기 링크로 들어온 사람은 편집 권한을 요청할 수 없다. 지금 design-abstract.md 구조로는 부족해 **추가 개발이 필요하다**: (1) 링크마다 다른 토큰·파라미터 발급·검증, (2) 보기 링크 사용자에게는 "편집 권한 요청" 버튼 자체를 숨기는 로직. 05-H-policy.md에서 "누가 할 수 있나·없나" 규칙으로 다시 정리한다 |

## 발산 질문 — 이 기능이 안 되는 경우 5개는?

> 아래는 팀이 아직 답하지 않은 후보를 코치가 채운 것이다. 전 행 `[제안]`. 채택·거절은 팀이 정한다.

| 대상 F | 안 되는 경우 후보 | 근거 |
| ----- | ---------------- | ---- |
| F5(AI 자동 생성) | AI 응답이 지연되어 타임아웃되는 경우 [제안] | 근거: R6(생성 실패·오류 시 안내 필요 요구의 연장) |
| F5(AI 자동 생성) | AI가 이미 폐업했거나 실제로 존재하지 않는 장소를 추천하는 경우 [제안] | 근거: R3(휴무일·영업시간 등 외부 데이터 정확성 문제와 같은 성격) |
| F5(AI 자동 생성) | 사용자가 입력한 조건들이 서로 모순되는 경우(예: 선택한 이동 방식으로 갈 수 없는 관심 장소) [제안] | 근거: R5 |
| F5(AI 자동 생성) | 외부 장소·지도 데이터 API 자체가 응답하지 않는 경우 [제안] | 근거: R3(외부 데이터 의존 구조) |
| F5(AI 자동 생성) | 같은 세션에서 생성 요청을 중복 클릭해 두 건 이상이 동시에 처리되는 경우 [제안] | 근거: R16(동시 수정 처리 규칙)에서 유추 |
| F13(동시 편집) | 두 A2가 같은 장소를 동시에 "삭제"와 "수정"으로 각각 조작하는 경우 [제안] | 근거: R16 |
| F13(동시 편집) | 편집 중 네트워크가 끊겼다가 복구되어 오래된 상태를 다시 전송하는 경우 [제안] | 근거: R16 |
| F13(동시 편집) | A1이 원본 일정을 삭제(F37 발생)하는 순간과 A2가 수정을 저장하는 순간이 겹치는 경우 [제안] | 근거: R43 |
| F13(동시 편집) | 같은 장소의 순서(우선순위)를 두 명이 동시에 서로 다른 값으로 바꾸는 경우 [제안] | 근거: R16 "별도의 충돌 처리 규칙은 두지 않는다"가 이 경우까지 포함하는지는 03에 명시되어 있지 않음 |
| F13(동시 편집) | 오프라인 상태에서 로컬로 수정한 뒤 재접속해 서버와 동기화하는 경우 [제안] | 근거: R16 |
| F9·F10(링크 공유·만료) | 편집 가능/보기 전용 권한을 착각해 잘못된 권한으로 링크를 공유하는 경우 [제안] | 근거: R11, design-abstract.md 권한 드롭다운(setPermission) |
| F9·F10(링크 공유·만료) | 이미 발급된 링크를 다시 발급(재발급)하려는 시도 [제안] | 근거: R12 "재발급·회수 절차는 없다" |
| F9·F10(링크 공유·만료) | 링크가 캡처·전달되어 의도하지 않은 제3자가 접근하는 경우의 접근 제어 [제안] | 근거: R11·R14 |
| F29~F34(게스트 승인 흐름) | A5가 로그인·회원가입 도중 이탈(중단)하는 경우 [제안] | 근거: R31·R32 |
| F29~F34(게스트 승인 흐름) | 여러 게스트가 동시에 편집 권한을 요청해 A1의 "권한 관리 모달" 요청 목록이 길어지는 경우 [제안] | 근거: R35 |
| F29~F34(게스트 승인 흐름) | 요청자가 승인 대기 중 다른 기기·브라우저로 같은 링크에 재접속하는 경우 [제안] | 근거: R33·R40 |
| F38(탈퇴) | A1이 탈퇴하는 시점에 다른 A2가 실시간으로 편집 중인 경우 [제안] | 근거: R44·R19 |
| F38(탈퇴) | 탈퇴 후 재가입 시 이전에 소유했던 일정에 대한 접근권 여부 [제안] | 근거: R44 |
| F38(탈퇴) | 여러 그룹 일정을 가진 A1이 한 번에 탈퇴하는 경우 일괄 처리 여부 [제안] | 근거: R44 |
| F11(AI 제안) | AI가 제안한 수정안을 사용자가 오래 확인하지 않고 방치하는 경우(제안의 유효기간 미정) [제안] | 근거: R13 새 문면(제안 확정 절차가 03에 없음) |
| F11(AI 제안) | 같은 대화창에서 제안이 연달아 여러 번 나와 사용자가 어떤 제안이 최신인지 헷갈리는 경우 [제안] | 근거: R13 |
| F11(AI 제안) | 사용자가 제안을 명시적으로 거부(반려)하고 싶을 때 거부하는 동작이 정해져 있지 않은 경우 [제안] | 근거: R13 새 문면의 "제안"이라는 단어가 수락/거부 개념을 함의하지만 03에 그 절차가 없음 |

## 발산 질문 — 같은 요구를 다른 방식으로 만족시키는 기능은?

> 아래도 전 행 `[제안]`이며, 팀이 채택하면 별도 F 번호를 새로 매겨야 한다(대상 R을 대체하는 것이 아니라 대안 후보다).

| 대상 R | 같은 요구를 다른 방식으로 만족시키는 기능 후보 | 근거 |
| ----- | ------------------------------------------- | ---- |
| R2 | 기준 선택 후 자동 계산 대신, 사용자가 드래그로 순서를 직접 재배열하는 수동 편집 기능 [제안] | 근거: R2·R15(수동 동선 수정 요구가 이미 존재, design-abstract.md에도 드래그 재정렬이 이미 있음) |
| R5 | AI 자동 생성 대신, 테마별 추천 코스(A7이 만든 게시물 기반) 템플릿을 골라 바로 적용하는 기능 [제안] | 근거: R5·R22·R23 |
| R17 | 자동 재계산 대신, 대안 동선 후보 2~3개를 제시하고 사용자가 직접 고르게 하는 기능 [제안] | 근거: R17 |
| R20 | 앱 안 후기 작성 대신, 외부 SNS 게시물을 자동 수집해 후기처럼 모아 보여주는 기능 [제안] | 근거: R20·R21 |
| R21 | 목록(피드) 형태 대신, 지도 위 마커로 후기 위치를 보여주는 기능 [제안] | 근거: R21 |
| R24, R25 | 관리자 수동 처리 대신, 신고 누적 건수가 기준을 넘으면 자동으로 숨김 처리하는 기능 [제안] | 근거: R24·R25 |
| R31, R32 | 로그인 후 승인 대기 대신, 링크에 포함된 1회용 초대 코드를 입력하면 승인 절차 없이 바로 편집 권한을 받는 기능 [제안] | 근거: R31·R32 |
| R13 | 자연어 대화 대신, 수정 가능한 항목을 체크리스트·폼으로 보여주고 선택하면 AI가 그 항목에 대한 제안을 만드는 기능 [제안] | 근거: R13(자연어 요청이 무엇을 수정할지 모르겠다는 불편에서 나온 요구이므로, 항목을 미리 보여주는 방식도 같은 불편을 해소할 수 있음) |

## 우선순위 2축

> 비고: 아직 팀이 중요도·MVP 여부를 정하지 않아 전 항목 `[?]`다. "노력(코치)" 도 팀 논의 전 코치가 임의로 추정하지 않고 비워 두었다(요청받지 않은 추정치를 앞서 채우지 않기 위함).

| F | 중요도(팀) | 노력(코치) | MVP / v2 | 이유 |
| - | -------- | -------- | -------- | ---- |
| F1 | [?] | [?] | [?] | [?] |
| F2 | [?] | [?] | [?] | [?] |
| F3 | [?] | [?] | [?] | [?] |
| F4 | [?] | [?] | [?] | [?] |
| F5 | [?] | [?] | [?] | [?] |
| F6 | [?] | [?] | [?] | [?] |
| F7 | [?] | [?] | [?] | [?] |
| F8 | [?] | [?] | [?] | [?] |
| F9 | [?] | [?] | [?] | [?] |
| F10 | [?] | [?] | [?] | [?] |
| F11 | [?] | [?] | [?] | [?] |
| F12 | [?] | [?] | [?] | [?] |
| F13 | [?] | [?] | [?] | [?] |
| F14 | [?] | [?] | [?] | [?] |
| F16 | [?] | [?] | [?] | [?] |
| F17 | [?] | [?] | [?] | [?] |
| F18 | [?] | [?] | [?] | [?] |
| F19 | [?] | [?] | [?] | [?] |
| F20 | [?] | [?] | [?] | [?] |
| F21 | [?] | [?] | [?] | [?] |
| F22 | [?] | [?] | [?] | [?] |
| F24 | [?] | [?] | [?] | [?] |
| F25 | [?] | [?] | [?] | [?] |
| F26 | [?] | [?] | [?] | [?] |
| F27 | [?] | [?] | [?] | [?] |
| F28 | [?] | [?] | [?] | [?] |
| F29 | [?] | [?] | [?] | [?] |
| F30 | [?] | [?] | [?] | [?] |
| F31 | [?] | [?] | [?] | [?] |
| F32 | [?] | [?] | [?] | [?] |
| F33 | [?] | [?] | [?] | [?] |
| F34 | [?] | [?] | [?] | [?] |
| F35 | [?] | [?] | [?] | [?] |
| F36 | [?] | [?] | [?] | [?] |
| F37 | [?] | [?] | [?] | [?] |
| F38 | [?] | [?] | [?] | [?] |

## 검토했으나 제외

| 후보 | 출처 | 제외 이유 |
| ---- | ---- | -------- |
| R10(구) — 보기 권한(A3)도 앱 안에서 희망 장소를 제안할 수 있어야 한다 | 03-H-requirements.md "검토했으나 제외" 표 · R10(구) | 03에서 이미 팀이 검토해 제외를 확정한 요구사항이라(02-H-workflow.md S6 "A3를 위한 '희망 장소 제안' 기능은 없음"과 배치) 04에서도 기능(F)으로 만들지 않음 |
| F23(구) — R26 "규정 위반" 판단 기준을 04 기능으로 도출 | 03-H-requirements.md R26 · 04 인터뷰(팀 결정) | 기능(트리거·입력·결과·예외)이 아니라 규칙(제약)이라 05-H-policy.md에서 다룰 사항으로 팀이 정함. 04에서 배제이지 요구 자체를 기각한 것은 아니며, 05 작성 시 다시 다룬다 |
| F15(구) — R18(운영시간·혼잡도 등 실시간 외부 변동 감지에 의한 자동 재조정)·R45(그 결과는 되돌리거나 거부할 수 없다는 제약) | 03-H-requirements.md R18, R45 · 04 인터뷰(팀 결정, 2026-09-16) | "해당 기능은 사용하지 않는 기능"이라고 팀이 정함 — 이번 기획 범위에서 아예 빼기로 결정. R17(사용자가 변수를 직접 골라 재계산하는 수동 방식)·F14는 그대로 유지된다 |
