> 문서: 04-Hello-features.md · 마지막 갱신: 2026-09-21

## 기능

| F | R | 트리거 | 입력 | 결과(실제 화면/동작) | 예외 처리(실제 코드 기준) | 정책(P) | 근거(경로) |
|---|---|---|---|---|---|---|---|
| F1 | R1,R2,R3 | "최적 동선 생성하기"(홈) 또는 "＋ 새 일정"(내 일정) 클릭 | 없음(로그인 상태) | 로그인 상태면 생성 방식(직접/AI) 선택 모달 → 날짜 선택 모달 순서로 진행 | 비로그인이면 클릭 즉시 `/login`으로 이동, 모달을 열지 않는다 | P14 | src/app/(main)/routes/NewTripFlow.tsx |
| F2 | R4,R5 | 주소 검색 실행, 지도 클릭, "현재 위치" 클릭 | 검색어 또는 지도 좌표 | Kakao 지오코딩 결과로 방문지가 목록·지도에 추가됨 | 주소 정보가 없으면 토스트 안내 후 임시 마커 제거, 좌표 조회 실패해도 방문지 자체는 추가됨, 지오위치 미지원/거부 시 토스트 안내 | P1 | src/app/planner/PlannerClient.tsx (`handleMapClick`, `openCurrentLocationAddConfirm`, `runMapSearch`) |
| F3 | R6,R7 | AI 마법사 5단계(지역→스타일→관심사→동행→이동수단) 진행 | 지역(또는 직접입력), 스타일 1개, 관심사 다중, 동행 1개, 이동수단 1개 | 각 단계 선택에 따라 "다음" 버튼 활성화, 마지막 단계에서 "AI 일정 생성하기" 실행 | 지역을 "기타"로 입력한 경우 Kakao 검색 결과가 없으면 "존재하지 않는 지역이에요" 안내 후 진행 차단 | P8 | src/app/(main)/routes/AiGenerateWizard.tsx |
| F4 | R8,R9 | F3의 "AI 일정 생성하기" 제출 | 지역/스타일/관심사/동행/이동수단/여행기간 | Gemini가 지정된 JSON 스키마로 방문지 목록을 생성하고, 서버가 체류시간(10-180분)·글자수·일차 범위를 정리(clamp)한 뒤 반환 | `GEMINI_API_KEY` 없으면 500 오류, Gemini 응답이 비정상이거나 최소 개수 미만이면 502와 "AI가 충분한 추천 장소를 만들지 못했어요" 반환 | P8 | src/app/api/route-generate/route.ts |
| F5 | R10 | 결과 화면 "다시 추천받기" 클릭 | 없음(직전 조건 재사용, 직전 장소명 제외 목록 자동 첨부) | 같은 조건으로 재생성하되 방금 나온 장소는 제외 요청에 포함됨 | F4와 동일 | P8 | src/app/(main)/routes/AiGenerateWizard.tsx (`generate`의 `excludeNames`) |
| F6 | R11 | 결과 화면 "이 일정으로 시작하기" 클릭 | 없음(현재 결과) | 결과를 세션 저장소에 담아 플래너로 이동, 플래너 진입 시 1회 소비 후 즉시 삭제 | 세션 저장소를 쓸 수 없는 환경(사생활 보호 모드 등)이면 조용히 무시(예외를 던지지 않음) | — | src/lib/ai-route-handoff.ts |
| F7 | R12,R13,R14,R52 | "경로 계산" 버튼 클릭, 또는 기준(최단시간/최단거리) 전환 | 방문지 좌표, 선택한 기준 | 같은 조건(출발지·좌표·변수)의 계산 결과가 캐시에 있으면 재사용, 없으면 Kakao 구간 데이터 + AI 가중치로 방문 순서를 최적화(좌표가 없는 방문지는 최적화 대상에서 제외) | 최적 순서 계산 중 개별 구간(Kakao) 조회가 실패하면 직선거리 추정치로 대체되어 계산이 중단되지 않음. 좌표는 있지만 아직 조회 전인 구간은 "경로 검색 버튼을 눌러주세요" 상태로 표시되고, 좌표가 없는 구간은 가짜 이동시간·거리·노선명 없이 "방문지 좌표가 없어 경로를 조회할 수 없습니다" 실패 상태로 표시됨 | P1, P15 | src/app/planner/PlannerClient.tsx (`runOptimalRoute`, `enrichedSegments`), src/lib/route-optimizer.ts |
| F8 | R15,R16 | "변수 추가" → 변수/세부/불편 정도 선택 → "AI로 동선 재구성" 클릭 | 선택한 지점(방문지), 변수 종류, 불편 정도 | 선택 지점 이후의 방문지만 Gemini에 보내 순서를 재배치 | 대상 방문지가 없으면 요청 자체를 보내지 않고 안내, AI 응답이 유효한 순열이 아니면 서버가 원래 순서를 그대로 돌려주고 "기존 동선을 유지했어요" 안내 | P1, P7 | src/app/planner/PlannerClient.tsx (`applySituationWithAi`), src/app/api/route-adjust/route.ts |
| F9 | R17,R18 | AI 도우미 FAB에 메시지 입력 후 전송 | 자연어 텍스트, 최근 대화 이력, 현재 경로 요약 | `/api/chat` 응답을 대화창에 표시, 장소 추가/교체 제안이 있으면 확인 팝업을 거쳐 하나씩 반영 | 응답 실패 시 "AI 응답을 가져오지 못했어요. 잠시 후 다시 시도해주세요." 안내, 제안 장소를 Kakao에서 찾지 못하면 토스트로 알리고 다음 제안으로 넘어감 | P1 | src/app/planner/PlannerClient.tsx (`sendAiMessage`, `advanceRecommendedQueue`), src/app/api/chat/route.ts |
| F10 | R19,R20 | 요약 바 "저장" 클릭 | 현재 방문지/구간/기준/날짜 | 새 일정이면 생성, 기존 일정이면 갱신 | 비로그인이면 로그인 모달, 저장 실패 시 "저장하지 못했어요" 토스트 | P1 | src/app/planner/PlannerClient.tsx (`saveOrRemoveAction`), src/app/api/schedules/route.ts, src/app/api/schedules/[id]/route.ts |
| F11 | R21,R22 | "＋ 일행 초대" → 권한(편집 가능/보기 전용) 선택 | 초대 권한 선택 | 역할별 토큰을 발급(이미 있으면 재사용)하고 링크를 보여줌, "복사" 클릭 시 클립보드에 복사 | 제작자가 아니면 403, 클립보드 복사 실패는 조용히 무시됨(안내 없음) | P2 | src/app/api/schedules/[id]/invite/route.ts, src/app/planner/PlannerClient.tsx (`copyInviteLink`) |
| F12 | R23 | 초대 링크(`/planner?invite=...`)로 접속 | 링크의 토큰 | 로그인 여부와 무관하게 해당 일정을 읽기 전용으로 표시(참여 처리는 하지 않음) | 토큰이 유효하지 않으면 404 "유효하지 않은 초대 링크예요" | P2 | src/app/api/schedules/invite/[token]/route.ts |
| F13 | R24,R25,R26 | 초대 링크 열람 화면에서 "저장"(참여) 클릭 | 없음(토큰은 URL에 이미 있음) | 실제 권한은 URL의 role이 아니라 일치한 토큰 컬럼으로 결정됨. 보기 링크는 즉시 뷰어로 등록, 편집 링크는 뷰어로 등록 + 편집 권한 요청 자동 생성(제작자에게 알림) | 비로그인이면 로그인 모달, 이미 편집자면 아무 변화 없이 그대로 유지 | P2, P3 | src/app/api/schedules/join/route.ts |
| F14 | R27 | 이미 뷰어로 참여 중인 사용자의 "편집 권한 요청" 클릭 | 없음 | 요청을 하나 생성하고 제작자에게 알림, 요약 바에 "승인 대기 중" 표시 | 이미 요청이 있으면 중복 생성하지 않음(조용히 무시) | P3 | src/app/api/schedules/[id]/edit-requests/route.ts |
| F15 | R28,R29 | 제작자가 "권한 관리" 모달에서 승인/거절 선택(확인 모달까지 2단계) | 승인/거절 선택 | 승인하면 협업자 역할이 editor로 바뀌고, 거절/승인 모두 요청 기록은 삭제되며 요청자에게 결과 알림 발송 | 제작자가 아니면 403, 요청을 찾지 못하면 404 | P3, P4 | src/app/api/schedules/[id]/edit-requests/[requestId]/route.ts |
| F16 | R30 | (자동) 뷰어가 플래너 화면을 열어 둔 상태 | 없음 | 5초 간격으로 최신 일정을 조회, 갱신 시각이 다르면 화면을 갱신하고 역할이 바뀌었으면 즉시 편집 화면으로 전환 | 편집자·제작자에게는 이 폴링이 동작하지 않음(뷰어 전용) | — | src/app/planner/PlannerClient.tsx (뷰어 전용 `setInterval`) |
| F17 | R31 | "내 일정" 카드 제목 클릭 또는 "일정 수정" 팝업 | 새 제목 텍스트 | 자신의 화면에서만 보이는 이름으로 저장됨(원본 `schedules.title`은 변경되지 않음) | 빈 문자열로 저장을 시도하면 반영되지 않음(트림 후 빈 값이면 무시) | — | src/app/(main)/routes/MyRoutesClient.tsx (`saveEdit`), src/db/schema.ts (`scheduleTitleOverrides`) |
| F18 | R32 | "일정 수정" 팝업의 날짜(달력) 변경 | 새 출발·도착 날짜 | 제작자에게만 달력 UI가 보이고, 저장 시 날짜가 반영됨 | 제작자가 아닌 사용자가 날짜를 함께 보낸 값은 서버가 조용히 무시(요청 자체는 실패시키지 않음) | P5 | src/app/(main)/routes/MyRoutesClient.tsx, src/app/api/schedules/[id]/route.ts (PATCH) |
| F19 | R33,R34 | "내 일정에서 삭제"/"원본 삭제" 클릭 → 확인 모달에서 확정 | 없음 | 제작자면 일정 자체와 모든 참여 기록이 함께 삭제, 편집자·뷰어면 자신의 참여 기록만 삭제(원본 유지) | 확인 모달에서 "취소"를 누르면 아무 일도 일어나지 않음 | P6 | src/app/api/schedules/[id]/route.ts (DELETE) |
| F20 | R35,R36,R37 | 여행지 탐색에서 지역 선택 후 카드의 "이 여행지로 일정 짜기" 클릭 | 선택한 지역, 여행 일수(당일치기/1박2일/2박3일 중 준비된 것) | 로그인 상태면 준비된 AI 동선을 곧바로 플래너(AI 모드)로 전달 | 비로그인이면 `/login`으로 즉시 이동(모달 없음), 준비된 동선이 없는 여행지는 버튼 자체가 표시되지 않음, 요청한 일수의 동선이 없으면 404 "아직 준비된 동선이 없어요" | P14 | src/app/(main)/explore/ExploreClient.tsx, src/app/api/explore/destinations/[id]/route.ts |
| F21 | R38,R39 | 커뮤니티 "여행 후기 쓰기"(또는 본인 글 "수정") | 사진(신규 글은 1-5장 필수), 여행지, 지역, 감상평(#태그 포함) | 새 글 추가 또는 기존 글 갱신, 사진은 4:3으로 크롭 후 저장(수정 시 사진은 변경 불가) | 비로그인이면 로그인 유도 모달, 사진 0장/지역·감상평 공백이면 저장 차단, 이미지가 아닌 파일은 필터링 | P9 | src/app/(main)/community/CommunityClient.tsx (`saveComposer`), src/app/api/community/posts/route.ts |
| F22 | R38 | 게시물/댓글/답글의 좋아요·북마크 버튼 클릭 | 없음 | 이미 눌렀으면 취소, 아니면 추가(토글) | 비로그인이면 로그인 유도 모달, 서버도 세션 없으면 401 | — | src/app/api/community/posts/[id]/like/route.ts, .../bookmark/route.ts |
| F23 | R38 | 댓글/답글 작성·삭제 | 댓글/답글 텍스트 | 댓글은 게시물 작성자에게, 답글은 원댓글 작성자에게 알림 생성. 답글은 댓글 아래 한 단계만 허용 | 작성자 본인이 아니면 삭제 403, 비로그인이면 401 | — | src/app/api/community/posts/[id]/comments/route.ts, src/app/api/community/comments/[id]/replies/route.ts, src/db/schema.ts (`postCommentReplies` 주석) |
| F24 | R38 | 검색어 입력(Enter), 지역 필터, 정렬(인기/최신/오래된), 목록·사진 보기 전환, "더보기" | 검색어, 지역, 정렬 기준 | 조건에 맞는 게시물을 필터링해 5개 단위로 더 보여줌(전량은 이미 한 번에 불러온 상태) | 조건에 맞는 게시물이 없으면 "검색 결과가 없어요"/"아직 게시물이 없어요" 표시 | — | src/app/(main)/community/CommunityClient.tsx |
| F25 | R40,R41 | (자동) 댓글/답글/편집권한 요청·승인·거절 발생, 사용자의 알림벨 클릭 | 없음 | 알림이 생성되고, 벨을 열면 그 시점의 모든 알림이 읽음 처리됨. 항목 클릭 시 관련 일정/게시물로 이동 | "내일 출발" 알림은 저장되지 않고 조회 시점에 계산되어 항상 읽음 상태로 표시됨 | — | src/lib/server/notifications.ts, src/components/layout/NotificationBell.tsx |
| F26 | R42 | 마이페이지에서 닉네임 변경 | 새 닉네임 | 형식(2-12자, 한글/영문/숫자/밑줄)과 중복·보호기간을 통과하면 세션 쿠키까지 갱신 | 형식 위반/중복/보호기간(7일) 위반 시 각각 다른 안내 메시지 반환 | P9 | src/lib/server/nickname.ts, src/app/api/account/nickname/route.ts |
| F27 | — | 마이페이지·회원가입에서 프로필 사진 업로드/삭제 | 이미지 파일 | 클라이언트에서 200×200 정사각형 JPEG로 리사이즈 후 데이터 URL로 저장(별도 스토리지 없음) | 이미지가 아닌 파일이면 차단, 데이터 URL이 형식/용량(약 2MB) 기준을 넘으면 서버가 거부 | P13 | src/lib/avatar-upload.ts, src/app/api/account/avatar/route.ts |
| F28 | — | 마이페이지 "알림 설정" 모달에서 항목별 토글 | 6개 알림 종류의 on/off | 전체 설정 객체를 한 번에 저장 | 저장 자체는 항상 성공하지만, "좋아요 알림"·"동선 알림"·"서비스 소식" 세 항목은 꺼도 켜도 실제 알림 생성에 영향을 주지 않는다(해당 알림을 만드는 코드 자체가 없음) | — | src/app/(main)/my-page/data.ts (`NOTIFICATION_DEFS`), src/lib/server/notifications.ts |
| F29 | R43,R44 | 마이페이지 "회원 탈퇴" → 확인 → (소유 일정 있으면 경고) → 최종 경고 → 탈퇴 | 동의 체크박스(단계별) | 소유한 일정이 없으면 곧바로, 있으면 목록 확인 후 강제 탈퇴 시 계정과 소유 일정·협업 기록이 모두 삭제됨 | 실패 시 일반 삭제는 첫 단계로, 강제 삭제는 최종 경고 단계로 되돌아가 오류 표시 | P10 | src/app/(main)/my-page/MyPageClient.tsx, src/app/api/auth/account/route.ts |
| F30 | R45 | 회원가입/비밀번호 재설정 중 "인증번호 받기" | 이메일 | 6자리 코드 발송(개발 환경에서 메일 자격 증명이 없으면 서버 로그로만 출력), 5분 내 인증 필요 | 60초 내 재요청은 차단, 5회 오답 시 "시도 횟수를 초과했어요", 만료 시 "만료되었어요" | P12 | src/lib/server/verification-code.ts, src/app/api/auth/verify-code/route.ts |
| F31 | R45 | 회원가입 폼 제출 | 이름, 아이디, 닉네임, 이메일(인증 완료), 비밀번호, 필수 약관 동의 | 계정 생성 후 자동 로그인 | 아이디 4자 미만/비밀번호 8자 미만/이메일 인증 30분 초과/중복된 아이디·닉네임·이메일 각각 별도 메시지로 차단 | P12 | src/app/(auth)/signup/SignupClient.tsx, src/app/api/auth/register/route.ts |
| F32 | R46 | 로그인 폼 제출 또는 구글 로그인 버튼 | 이메일/비밀번호, 또는 구글 자격 증명 | 세션 쿠키 발급 후 이전 화면 또는 홈으로 이동 | 미가입 이메일/구글 전용 계정에 비밀번호 로그인 시도/비밀번호 불일치 각각 별도 401 메시지 | P11 | src/app/api/auth/login/route.ts, src/app/api/auth/google/route.ts |
| F33 | R45 | "비밀번호를 잊으셨나요?" → 이메일 → 인증번호 → 새 비밀번호 | 이메일, 인증번호, 새 비밀번호 | 인증 성공 시 비밀번호 변경과 동시에 자동 로그인 | 미가입/구글 전용 계정은 재설정 자체를 막고 안내 | P12 | src/app/(auth)/forgot-password/ForgotPasswordClient.tsx, src/app/api/auth/password-reset/* |
| F34 | R46 | 로그아웃 클릭 | 없음 | 세션 쿠키 삭제 | — | P11 | src/app/api/auth/logout/route.ts |
| F35 | R47 | 로그인 화면 "아이디 저장" 체크 | 체크 여부 | 이메일 값만 로컬 저장소에 남겨 다음 방문 시 자동 채움 | 비밀번호는 저장하지 않음, 로그아웃해도 이 값은 지우지 않음 | — | src/app/(auth)/login/LoginClient.tsx |
| F36 | R49 | 플래너 화면 진입, 같은 브라우저에 투어 완료·건너뛰기 기록이 없음 | 없음(화면 내 요소를 스스로 탐지) | 9개 요소를 순서대로 스포트라이트로 짚어주는 말풍선 안내가 뜨고, "다음/이전/건너뛰기/완료" 버튼으로 진행하며 마지막에 완료 처리되면 로컬 저장소에 기록을 남겨 다시 뜨지 않게 함 | 이미 열려 있는 다이얼로그(예: 새 일정 날짜 모달)가 있으면 투어를 시작하지 않고 대기하고, 대상 요소를 6초(200ms×30회)간 하나도 찾지 못하면 이번엔 띄우지 않되 완료 처리는 하지 않아 다음 방문 때 다시 시도함 | — | src/app/planner/ProductTour.tsx |
| F37 | R48 | 회원가입 성공(F31) 직후 | 없음(가입자 이메일·이름) | "[p:nder] 회원가입을 환영합니다 🎉" 제목의 환영 이메일 발송 | `GMAIL_USER`/`GMAIL_APP_PASSWORD`가 없으면 실제 발송 없이 서버 로그로만 남고, 자격 증명이 있어도 발송 자체가 실패하면 로그만 남기고 예외를 던지지 않아 회원가입 응답에는 전혀 영향이 없음(F31의 성공 응답이 그대로 나감) | SP3 | src/app/api/auth/register/route.ts, src/lib/server/email.ts (`sendWelcomeEmail`, `sendMail`) |
| F38 | R50,R51 | (자동) F4/F5 응답을 받은 직후 | AI가 생성한 장소명·주소 힌트·선택 지역 | 카카오 키워드 검색으로 지역이 일치하는 후보만 좌표로 인정하고, 매칭에 실패한 장소는 같은 조건으로 최대 3라운드까지 AI에게 대체 후보를 다시 요청해 그 자리를 채운다 | 3라운드가 지나도 못 찾은 장소가 있으면 "좌표 매칭에 실패했습니다" 모달을 표시한다(일부만 실패 시 "해당 장소는 일정에서 제외했어요", 전부 실패 시 "조건을 바꾸거나 다시 시도해주세요"이며 이 경우 결과 화면으로 넘어가지 않고 마법사 단계에 머무른다) | P15 | src/app/(main)/routes/AiGenerateWizard.tsx (`resolvePlaceCoords`, `matchesRegion`, `unmatchedNotice`) |

## 우선순위 추정

| F | 코드 성숙도(완성/부분/흔적) | MVP 추정 여부 | 판단 근거 |
|---|---|---|---|
| F1 | 완성 | 예 | 01문서의 핵심 문제(동선 계획 진입)를 여는 첫 단계이며 서버·클라이언트 모두 동작함 |
| F2 | 완성 | 예 | 수동 일정 제작의 핵심 동작이며 검색/지도클릭/현재위치 세 경로 모두 구현됨 |
| F3 | 부분 | 예 | 입력 UI는 완성이나, 첫 생성 실패 시 오류 문구가 화면에 표시되지 않는 결함이 있음(근거: `AiGenerateWizard.tsx`의 결과 유무에 따른 조건부 렌더링) |
| F4 | 완성 | 예 | Gemini 연동·응답 검증·안전한 기본값 처리까지 서버에 갖춰져 있음 |
| F5 | 완성 | 아니오 | 핵심 생성 기능(F4)의 보조 기능으로, 없어도 F4만으로 서비스가 성립함 |
| F6 | 완성 | 예 | AI 생성과 수동 편집을 잇는 필수 연결 고리(01문서의 차별점) |
| F7 | 완성 | 예 | 핵심 기능이며, 좌표 없는 구간에서 가짜 이동시간·거리·지하철 노선명을 만들어 보여주던 목업 코드(`route-engine.ts`의 `computeMockSteps`/`SEGMENT_DISTANCES`)가 삭제되고 정직한 실패 상태 표시로 대체됨(근거: 커밋 8d81aaa, src/app/planner/PlannerClient.tsx `enrichedSegments`) |
| F8 | 완성 | 아니오 | 상황 변수 반영은 부가 기능이며, 없어도 기본 경로 계산(F7)은 성립함 |
| F9 | 완성 | 아니오 | AI 도우미는 참고용 대화 기능으로 핵심 동선 생성·편집 경로가 아님 |
| F10 | 완성 | 예 | 저장 없이는 공유·재방문 자체가 불가능한 핵심 기능 |
| F11 | 완성 | 예 | 01문서의 "보기/편집 링크 분리" 차별점을 구현하는 핵심 기능 |
| F12 | 완성 | 예 | 비로그인 열람은 공유 시나리오의 기본 전제 |
| F13 | 완성 | 예 | 공유받은 사람이 실제로 참여하는 유일한 경로 |
| F14 | 완성 | 아니오 | F13에서 이미 자동 생성되는 편집 요청의 대안 경로로, 없어도 F13이 성립함 |
| F15 | 완성 | 예 | 편집 권한 부여의 유일한 경로 |
| F16 | 완성 | 아니오 | 사용성 개선(수동 새로고침 불필요)이며 없어도 뷰어는 재접속으로 최신화 가능 |
| F17 | 완성 | 아니오 | 표시 편의 기능으로 원본 동작에는 영향 없음 |
| F18 | 완성 | 아니오 | 날짜 수정은 초기 생성(F1) 이후의 보조 편집 기능 |
| F19 | 완성 | 예 | 데이터 정리를 위한 필수 기능이며 역할별 분기까지 구현됨 |
| F20 | 완성 | 아니오 | 여행지 탐색은 핵심 문제(동선 계획)로 들어가는 보조 진입로이며, 사전 준비된 동선이 없으면 비활성화됨 |
| F21-F24 | 완성 | 아니오 | 커뮤니티는 01문서의 핵심 문제(동선 계획)와 직접 관련이 없는 부가 기능이며, 읽기 권한이 서버·화면에서 서로 다르게 처리되는 결함이 있음 |
| F25 | 부분 | 아니오 | 알림 생성 자체는 동작하나, 알림 설정 화면의 절반 가까운 항목이 실제로는 아무 알림도 만들지 않음(F28과 연결된 결함) |
| F26, F27 | 완성 | 아니오 | 계정 부가 설정으로 핵심 동선 기능과 무관 |
| F28 | 부분 | 아니오 | 설정 UI는 있으나 6개 중 3개 토글이 실제 알림 생성에 아무 영향이 없음 |
| F29 | 완성 | 아니오 | 계정 관리 기능이며 단계별 확인까지 구현되어 있으나 핵심 문제와는 무관 |
| F30-F35 | 완성 | 예 | 로그인·회원가입 없이는 일정 저장·공유(F10·F11)가 불가능하므로 인증 체계 전체가 사실상 MVP 전제 조건임 |
| F36 | 완성 | 아니오 | 처음 오는 사용자를 위한 사용성 개선 안내 기능이며, 없어도 핵심 일정 생성·편집 기능은 그대로 동작함 |
| F37 | 완성 | 아니오 | 가입 후 부가 안내 메일이며 01문서의 핵심 문제(동선 계획)와 무관하고, 발송 실패해도 가입 자체에는 영향이 없음 |
| F38 | 완성 | 예 | AI 생성(F3/F4)의 결과가 실제로 신뢰할 수 있는 좌표를 갖추도록 보장하는 장치로, 이게 없으면 AI 생성 기능 자체가 잘못된 위치·이동정보를 조용히 보여주는 결함으로 되돌아감(01문서의 핵심 차별점인 AI 자동 생성의 신뢰성과 직결) |
