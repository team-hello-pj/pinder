> 문서: 12-Hello-evaluation-mapping.md · 근거: 코드베이스 분석 · 마지막 갱신: 2026-09-18

> 안내: 이 프로젝트가 실제로 사용하는 외부 채점 기준 원문(평가표 파일)은 이 저장소(plan/, docs/, 레포 루트) 어디에서도 찾지 못했다 — 근거 없음. 따라서 아래 표는 특정 평가표를 인용한 것이 아니라, 기획서·디자인 명세서 평가에 일반적으로 쓰이는 관점(문제 정의의 근거, 요구-기능-정책 추적성, 예외 처리 완결성, 디자인 토큰화·일관성 등)을 기준으로 01-11 문서가 실제로 무엇을 증명했고 무엇을 증명하지 못했는지를 있는 그대로 기록한 것이다. 부족한 항목을 숨기거나 미화하지 않는다.

## 기획 채점 대응

| 채점 항목(일반적 평가 관점) | 충족 여부 | 근거 문서·항목 | 비고(부족하면 왜 부족한지) |
|---|---|---|---|
| 문제 정의가 유사 서비스 비교 등 근거에 기반하는가 | 충족 | 01문서 "유사 서비스 비교", "문제 상황 설명" | 팀이 기존에 조사한 plan/plan-example/01-merged.md 내용을 기반으로 재구성한 것이며, 이번 세션에서 신규 시장조사를 수행하지는 않았다 |
| 행위자(A)가 실제 데이터 모델과 일치하는가 | 충족 | 02문서 행위자 표 | `scheduleCollaborators.role`(editor/viewer) 등 실제 스키마와 1:1로 대응시켰다 |
| 업무 흐름(S)이 실제 화면 전환·API 호출과 일치하는가 | 충족 | 02문서 흐름도, 08문서 다이어그램 | "일정 종료 후 읽기 전용 전환"처럼 예전 기획에는 있었지만 코드에 없는 항목은 포함하지 않고 "부재 확인됨"으로 명시해 정합성을 지켰다 |
| 요구사항(R)→기능(F)→정책(P)이 서로 추적 가능한가 | 충족 | 03·04·05문서의 R/F/P ID 상호 참조 | 뒤 문서가 앞 문서에 없는 ID를 새로 만들지 않았다 |
| 예외·오류 상황이 문서화되어 있는가 | 부분 충족 | 09문서 예외 흐름표(32건) | 예외 대부분이 소스 코드 확인 수준에 머문다 — 배포 링크는 GET 열람만 허용되는 규칙이라 폼 제출·오류 트리거 같은 실제 상호작용 검증과 화면 캡처는 하지 못했다 |
| 정책·상태 전이가 DB 스키마와 일치하는가 | 충족 | 05문서 "상태값 전이" | src/db/schema.ts의 실제 컬럼·FK 제약과 대조해 작성했다 |
| 우선순위(MVP 추정) 판단이 코드 성숙도에 근거하는가 | 충족 | 04문서 "우선순위 추정" | 판단 근거를 코드 완성도와 01문서의 핵심 문제 연관성으로 명시했다. 다만 MVP 여부 자체는 "추정"이므로 해석의 여지가 있는 항목(예: 여행지 탐색)도 있다 |
| 실제로 존재하지 않는 기능(관리자 화면, 신고 처리 등)을 지어내지 않았는가 | 충족 | 02문서 행위자 표의 각주, 03문서 "검토했으나 제외" | plan/plan-example에 있던 "A7(에디터의 테마 게시물 작성)"·관리자 역할을 코드에서 찾지 못해 포함하지 않았다 |
| 발견된 설계 결함·모순을 숨기지 않고 드러냈는가 | 충족 | 09문서의 "결함으로 판단" 표시 5건, 05문서 SP2·SP3 | 편집 권한 요청 실패 시 무안내, 삭제 확인 모달 문구 불일치, 알림 설정 일부 미작동, SESSION_SECRET 누락 시 전면 장애 등을 그대로 기록했다 |
| 대상 사용자의 규모·이용 빈도 등 정량 데이터가 뒷받침되는가 | 미흡 | 01문서 | plan/plan-example/memo.md에도 이런 정량 데이터가 없고, 코드에서도 확인할 수 있는 성격의 정보가 아니다 — 코드 기반 리버스 기획 방법론 자체의 한계다 |

## 디자인 채점 대응

| 채점 항목(일반적 평가 관점) | 충족 여부 | 근거 문서·항목 | 비고 |
|---|---|---|---|
| 색상 팔레트가 라이트/다크 모두 토큰화되어 있는가 | 부분 충족 | 10문서 "색상" | 토큰(`tokens.css`) 자체는 라이트/다크 모두 잘 정의되어 있으나, 화면 단위 CSS 모듈 90여 곳에서 토큰 대신 하드코딩된 hex 값이 발견되어 실제 적용은 전면적이지 않다 |
| 타이포그래피가 체계적인 스케일로 정의되어 있는가 | 미흡 | 10문서 "타이포그래피" | 중앙 집중식 크기/굵기 토큰이 없고, 화면·컴포넌트마다 픽셀 값을 개별적으로 정한다 |
| 아이콘 시스템이 일관되어 있는가 | 미흡 | 10문서 "아이콘" | PNG·이모지·인라인 SVG·유니코드 기호 네 가지 방식이 혼용되며, 같은 토글(ThemeToggle)의 두 상태가 서로 다른 방식을 쓰는 사례까지 있다 |
| 컴포넌트가 hover/disabled/focus 상태까지 구현되어 있는가 | 부분 충족 | 11문서 컴포넌트 인벤토리 | 대부분 hover는 있으나, disabled·`:focus-visible` 전용 스타일은 Modal·DateRangeCalendar 등 다수 컴포넌트에서 빠져 있다 |
| 설계된 공용 컴포넌트가 실제 화면에서 재사용되는가 | 미흡 | 11문서 컴포넌트 인벤토리 | `Card`, `SegmentedControl`, `ScreenScaffold` 세 컴포넌트가 정의만 되어 있고 `src/app/` 어디에서도 사용되지 않는다(죽은 컴포넌트) |
| 다크모드가 전체 화면에 일관되게 적용되는가 | 부분 충족 | 10문서 "토큰 규칙 위반" | 토큰 시스템은 다크모드를 지원하지만, `my-routes.module.css`·`planner.module.css`는 다크모드에서 어긋나는 라이트 전용 고정값을 코드 스스로 주석으로 인정하고 있다 |
| 반응형(모바일 대응)이 구현되어 있는가 | 부분 충족 | 06·10문서, src/components/layout/MobileMenu.tsx, src/app/planner/planner.module.css | 헤더·메뉴 수준의 반응형 분기와 플래너 하단 액션바의 모바일 그리드 재배치(12분할 그리드, `grid-column: span`)까지 코드로 확인되지만, 화면 전체가 400px 폭에서 실제로 어떻게 보이는지는 스크린샷을 확보하지 못해 시각적으로 검증하지 못했다 |
| 동일 기능의 UX가 화면 간에 일관되는가 | 미흡 | 11문서 "추가로 확인된 사실"(이미지 업로드) | 같은 "이미지 업로드" 기능인데 회원가입·마이페이지의 아바타는 자동 정중앙 크롭, 커뮤니티 사진은 사용자가 직접 조정하는 크롭 모달로 서로 다른 방식을 쓴다 |
| 화면 설계서가 실제 배포 화면과 대조 검증되었는가 | 부분 충족 | 06·08문서 상단 안내, 06문서 SC09 "정정 사항" | 이미지 캡처 도구가 없어 시각적 스크린샷 대조는 하지 못했지만, `curl` GET 응답의 텍스트 노드를 코드와 직접 대조하는 방식으로 9개 화면 전부를 검증했다. 그 결과 SC03·SC07은 클라이언트 렌더링이라 초기 응답에 내용이 없다는 사실을 확인했고, SC09(마이페이지)는 애초 문서 초안의 "로그인 상태에서만 도달한다"는 서술이 틀렸다는 것(실제로는 비로그인도 화면 전체가 열림)을 이 검증 과정에서 발견해 정정했다 — 즉 검증 자체는 실효성이 있었지만, 여전히 실제 시각적 레이아웃(색상·배치·반응형 여부)까지 확인한 것은 아니다 |
